<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============ ISDesk-Menue-Theme (dunkel, passend zur Fenster-Optik) ============ -->

    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#F51E2128" />
    <SolidColorBrush x:Key="MenuBorderBrush" Color="#3DFFFFFF" />
    <SolidColorBrush x:Key="MenuForegroundBrush" Color="#F2FFFFFF" />
    <SolidColorBrush x:Key="MenuHighlightBrush" Color="#2EFFFFFF" />
    <SolidColorBrush x:Key="MenuSeparatorBrush" Color="#26FFFFFF" />
    <SolidColorBrush x:Key="MenuAccentBrush" Color="#7DB7FF" />

    <Style TargetType="ContextMenu">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource MenuForegroundBrush}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Margin="8"
                            Background="{StaticResource MenuBackgroundBrush}"
                            BorderBrush="{StaticResource MenuBorderBrush}" BorderThickness="1"
                            CornerRadius="8" Padding="5">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.45" Color="Black" />
                        </Border.Effect>
                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource MenuForegroundBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="5"
                                Padding="8,6" Margin="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon"
                                                  HorizontalAlignment="Left" VerticalAlignment="Center" />
                                <TextBlock x:Name="CheckGlyph" Grid.Column="0" Text="✓"
                                           FontSize="12" FontWeight="Bold" VerticalAlignment="Center"
                                           Foreground="{StaticResource MenuAccentBrush}"
                                           Visibility="Hidden" />
                                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                  VerticalAlignment="Center" RecognizesAccessKey="True" />
                                <TextBlock x:Name="Arrow" Grid.Column="2" Text="›"
                                           FontSize="14" Margin="10,0,0,0" VerticalAlignment="Center"
                                           Foreground="#AAFFFFFF" Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <Popup x:Name="SubMenuPopup" Placement="Right"
                               IsOpen="{TemplateBinding IsSubmenuOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                            <Border Margin="8"
                                    Background="{StaticResource MenuBackgroundBrush}"
                                    BorderBrush="{StaticResource MenuBorderBrush}" BorderThickness="1"
                                    CornerRadius="8" Padding="5">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.45" Color="Black" />
                                </Border.Effect>
                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle" />
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource MenuHighlightBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Trennlinien innerhalb von Menues (Lookup ueber SeparatorStyleKey) -->
    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="{StaticResource MenuSeparatorBrush}" Margin="10,4" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
